# Memory — Order Tracking (16) Built From Spec + Review-Finding Fixes

Last updated: 2026-08-07

## What was built

- Read and implemented `context/feature-specs/16-ORDER-TRACKING.md` from scratch on branch `order-tracking` — a genuine new build, not a gap-closing pass (only `/account/orders/[id]` existed before, as the post-checkout confirmation screen; see prior session's "Cart and Flutterwave checkout" entry). Did the full AGENTS.md-mandated context read first (PROJECT_OVERVIEW/PRD/BUSINESS_RULES/ROADMAP, then API_ROUTES/DATABASE_SCHEMA/FOLDER_STRUCTURE/CODING_STANDARDS) before writing any code.
- `gifvtme_migration_019_order_tracking.sql`: adds `tracking_number`/`tracking_url`/`carrier_name`/`estimated_delivery`/`status_change_notes` to `orders`; formally owns `order_status_history` (`CREATE TABLE IF NOT EXISTS` + `ADD COLUMN IF NOT EXISTS` for `customer_notified`/`notes`/`retry_count`/`permanently_failed`/`claimed_at`, same pattern as migrations 015/016); adds the `on_order_status_changed` and `validate_order_status_transition` triggers (backward/skip-step guard, implemented verbatim from the spec's own transition map); adds owner-scoped RLS on `order_status_history`; fixes `orders.wishlist_item_id`'s FK to `ON DELETE SET NULL` (spec Edge Case #6).
- `lib/orders/`: `types.ts`, `statusTransitions.ts` (+ unit tests — `isValidOrderStatusTransition` mirrors the DB trigger), `buildOrderStatusEmail.ts` (+ unit tests, per-status copy taken directly from spec), `server.ts` (`getOrdersForUser`/`getOrderDetail`).
- `lib/email/resend.ts`: added `sendOrderStatusEmail`.
- API: `GET /api/orders`, `GET /api/orders/[id]`, `POST /api/orders/notify` (cron, same atomic-claim/retry/`permanently_failed` pattern as `/api/reminders`/`/api/thank-you/process`).
- `vercel.json`: added `/api/orders/notify` on a **daily** cron (`30 6 * * *`) — Vercel Hobby plan only allows daily schedules, same constraint as reminders/thank-you.
- UI: `components/order/OrderStatusBadge.tsx`, `OrderCard.tsx`, `OrderList.tsx` (Active/Completed/Cancelled tabs), `OrderTracking.tsx` (4-step GSAP-pulsed tracker, red banner for cancelled/refunded), `TrackingLink.tsx`; `app/account/orders/page.tsx` (new list page); rebuilt `app/account/orders/[id]/page.tsx` around the full tracking view (tracker, summary, shipping, tracking section, expandable `<details>` history timeline).
- Deleted `components/order/OrderConfirmationScreen.tsx` (confirmed no other callers first) — the rebuilt detail page now covers that same "just confirmed" moment as one of its tracker states.
- Added `warning`/`info` variants to `components/ui/Badge.tsx` (amber/blue, for the order status badge).
- Updated context docs in the same change: `API_ROUTES.md`, `DATABASE_SCHEMA.md`, `ROADMAP.md`, `FOLDER_STRUCTURE.md`, `COMPONENT_LIBRARY.md`, `ui-registry.md`, `15-CART-CHECKOUT.md` (its `/account/orders/[id]` description), and `16-ORDER-TRACKING.md` itself (new "Implementation notes" section documenting every divergence instead of a full rewrite, since this was a from-spec build that matched closely).
- Three follow-up review-finding fixes, each individually re-verified against current code before fixing (per [[feedback-verify-and-fix-findings-workflow]]):
  1. `app/api/orders/notify/route.ts`: reduced `BATCH_LIMIT` from 50 to 10 — serial (not concurrent) processing with no `maxDuration` override risked exceeding Vercel Hobby's 10s default execution limit.
  2. `FOLDER_STRUCTURE.md`: added the missing `account/orders/` list route and `TrackingLink` to the `order/` component inventory (both existed in code but weren't documented).
  3. `ROADMAP.md`: removed a stale "Actual Resend email sending for order status — not started" bullet now that `/api/orders/notify` genuinely sends.
- Two commits landed this session: `59ccb27` ("feat: implement order tracking and fulfillment features") and `38fab4a` ("feat: enhance order tracking and email notifications with error handling and HTTPS validation"). **Neither commit nor the push/merge was done by me** — the developer committed, pushed, and merged (`origin/order-tracking` → PR #29 → `main`) between turns, applying some of my edits directly plus their own additions: `38fab4a` includes an `isHttpsUrl()` guard in `buildOrderStatusEmail.ts` (only trusts `https:` tracking URLs, falls back to the order link otherwise), `escapeHtml()` around the CTA URL itself in `ctaButton()`, `aria-pressed` on `OrderList.tsx`'s tab buttons, and a `pendingOrderError` check added to the fallback query in the order detail page — none of which I wrote.

## Decisions made

- Confirmed [[feedback-spec-vs-architecture-precedence]] applies even to an explicit "read and implement exactly as specified" instruction — full context-file audit came first regardless of the literal instruction's wording.
- The spec's transition-map SQL was implemented **verbatim** despite an internal inconsistency (Functional Requirement #3's prose allows cancellation "from any non-terminal status"; the literal map doesn't allow `cancelled` from `pending_payment`/`payment_failed`) — documented the inconsistency rather than silently resolving it by guessing intent. **Resolved 2026-08-12**, in a later session: asked the developer directly rather than guessing; they confirmed the broader FR3 rule. `gifvtme_migration_021_cancel_from_pending.sql` now allows `pending_payment`/`payment_failed` → `cancelled`, with `lib/orders/statusTransitions.ts` and `16-ORDER-TRACKING.md`'s own map updated to match.
- Added `retry_count`/`permanently_failed`/`claimed_at` to `order_status_history` beyond the spec's literal Database Changes SQL block — justified because the spec's own Edge Case #4 requires equivalent behavior, and every other cron in this repo (reminders, thank-you) already uses this exact concurrency pattern.
- `refunded` orders were placed in the Cancelled tab and `delivered`'s email CTA points at the order instead of a review page — both are judgment calls where the spec was silent or referenced an unbuilt feature (reviews), documented as explicit assumptions rather than left silent.

## Problems solved

- Vercel Hobby plan only allows daily cron schedules (see [[project-vercel-hobby-cron-limit]]) — `/api/orders/notify` scheduled daily instead of the spec's every-5-minutes, matching the existing reminders/thank-you precedent.
- A pasted review finding correctly identified that `/api/orders/notify`'s serial processing loop (`BATCH_LIMIT=50`, no `maxDuration` override, no concurrency unlike `/api/thank-you/process`) risked exceeding Vercel Hobby's 10s default execution window even in the typical case. Fixed by reducing `BATCH_LIMIT` to 10, reasoning documented inline — a row that doesn't finish in time is safely retried once its claim goes stale, so under-sizing the batch for the common case (rather than the worst case) is fine.

## Current state

- Branch `order-tracking` (tracks `origin/order-tracking`), working tree clean.
- **This work is already merged to `main`** — `origin/main` includes it via merge commit `2a81ee7` ("Merge pull request #29 from givftme/order-tracking"). The local branch has nothing further to push.
- `tsc`, `eslint`, `npm test` (127/127) all clean as of the last check on this branch.
- **`gifvtme_migration_019_order_tracking.sql` is applied but broken — this blocks any production release of order status changes (Retool or otherwise) until `gifvtme_migration_022_order_status_history_columns.sql` is also applied.** Verified 2026-08-12 in a later session via read-only REST calls against the live Supabase project using the service-role key already present in `.env.local` (project host `kfhufngnaibunciudkmp.supabase.co` — environment otherwise unconfirmed, exact applied timestamp/commit not knowable from a schema check alone, no migration-history table exposed via the REST API). Confirmed applied: `orders`' tracking columns, and `order_status_history`'s `customer_notified`/`notes`/`retry_count`/`permanently_failed`/`claimed_at`. **Confirmed broken:** `order_status_history` pre-existed with `old_status`/`new_status`/`changed_by`/`created_at` instead of the `status`/`changed_at` migration 019 assumed — those two were only declared inside a `CREATE TABLE IF NOT EXISTS` block that silently no-opped against the pre-existing table, never added as their own `ADD COLUMN IF NOT EXISTS`. `handle_order_status_changed()` inserts into `(order_id, status, changed_at, notes)`, so it will fail with a real Postgres error — rolling back the whole order update — the instant any order's status actually changes. Very likely never fired for real yet since Retool isn't set up against this project (see "Not started" below), but will break the first Retool status change otherwise. Migration 022 adds the missing columns (nullable, best-effort backfilled from `new_status`/`created_at`) — **developer confirmed applied 2026-08-12**, re-verified via the same read-only checks (`status`/`changed_at` now present).
- **The same class of bug hit checkout, found while setting up Retool.** Live-testing checkout to populate the Retool orders queue produced `POST /api/checkout` 502s with `PGRST204: Could not find the 'payment_claimed_at' column`. Migration 018 (`gifvtme_migration_018_checkout_atomic_order.sql`) was only partially applied: `gifvtme_create_checkout_order()` + its `GRANT` existed and worked (verified via a safe read-only RPC probe — sent parameters matching its signature so it hit its own `auth.uid()` guard and raised that specific exception, without inserting anything), but `ALTER TABLE orders ADD COLUMN IF NOT EXISTS payment_claimed_at` had not run. No new migration needed — 018 is already fully idempotent, so re-running the whole file (developer did this) fixed it. Separately found and fixed while debugging: `POST /api/checkout` and `lib/checkout/reinitiatePayment.ts` both swallowed the real Flutterwave rejection reason (never logged `payment.error`, or fell back to a useless generic string) — added `console.error` calls at both sites, and rewrote `initiateFlutterwavePayment` (`lib/flutterwave/index.ts`) to read the raw response body and surface `Flutterwave HTTP {status}: {rawBody}` when there's no `message` field. That immediately revealed the real (non-application) cause: a Cloudflare "Attention Required" 403 in front of Flutterwave's API, specific to the developer's local network — a bare curl to the same endpoint from this session's own sandbox got a clean 401 straight from Flutterwave, ruling out a blanket client-fingerprint block. **Deliberately deferred, not further debugged**: developer chose to verify Flutterwave connectivity once deployed to Vercel (different IP range) rather than chase a local-network-specific block.
- **`order_status_history` was still broken after migration 022 — a third gap.** Manually moving a test order to `confirmed` (to unblock Retool testing) hit `null value in column "new_status" ... violates not-null constraint`. Migration 022 only fixed `status`/`changed_at`; `new_status` (part of the table's pre-existing shape) is `NOT NULL` with no default and the trigger never supplied it. This time confirmed via PostgREST's own OpenAPI schema (`GET {SUPABASE_URL}/rest/v1/`, parsed as JSON) instead of one-off column probing — gives the complete, authoritative column list + nullability + defaults in one call; use this method first for any future live-schema question rather than guessing column-by-column. Full confirmed live column set: `id, order_id, changed_by, old_status, new_status, note, created_at, customer_notified, notes, retry_count, permanently_failed, claimed_at, status, changed_at`. `gifvtme_migration_023_order_status_history_new_status.sql` fixes it (trigger now also sets `old_status`/`new_status`) — **developer confirmed applied and working 2026-08-13**, re-verified: `orders` has a row in `confirmed`, `order_status_history` has a matching row with `old_status: pending_payment`/`new_status: confirmed` and both timestamps populated. This is the third `order_status_history` migration in two days (019 → 022 → 023); `confirmed` working doesn't prove every transition path is clean — stay alert the first time `shipped`/`cancelled`/etc. actually fires for real.
- **Unexplained: `order_status_history.status` mutates on already-written rows.** After a second real transition (`confirmed → under_review`), the *first* history row's `status` silently changed from `confirmed` (correct immediately after being written) to `under_review` (the order's new current status) — while that row's `old_status`/`new_status` stayed correct. Exhaustively ruled out, each checked live: Retool's own queries (only one write query, only touches `orders`), a rogue trigger (`information_schema.triggers` — exactly the 4 expected, no more), Postgres RULES (none), a generated column (confirmed plain), `pg_cron` (not installed), manual SQL (developer confirmed none run), and the live trigger function body itself (`pg_get_functiondef()` matches migration 023 verbatim). No cause found. Applied a **defensive fix, not a root-cause fix**: every app read of this table now selects `status:new_status` (PostgREST aliasing, JSON shape unchanged) instead of the unreliable `status` column, since `new_status` has been correct in every observation — `lib/orders/server.ts`'s `getOrderDetail` and `app/api/orders/notify/route.ts` (both the select **and** its `.in("status", ...)` filter, which needed its own separate fix since aliasing doesn't change what a filter queries). This closes a real risk: the notify cron could have silently skipped a customer-facing status email or sent the wrong template if a row's `status` drifted before the cron ran. **Not checked**: whether `notes`/`changed_at`/`customer_notified`/`retry_count`/`permanently_failed`/`claimed_at` (same category of migration-022-added column) are subject to the same mutation. If this ever recurs on a *different* column, that would rule out something status-specific and point at something structural — worth digging further at that point.
- **Legacy `old_status`/`new_status`/`changed_by`/`note` columns dropped, not root-caused.** Developer's call: stop carrying two overlapping status models — drop the pre-existing legacy columns entirely rather than keep working around them. `gifvtme_migration_024_drop_legacy_status_columns.sql` drops all four and restores `handle_order_status_changed()` to the literal single-column trigger the spec always specified (no more populating `old_status`/`new_status` to satisfy a constraint that no longer exists). The defensive `status:new_status` aliasing from the entry above was reverted back to plain `status` in `lib/orders/server.ts` and `app/api/orders/notify/route.ts` in the same change, since `new_status` won't exist post-migration. **Developer confirmed applied 2026-08-17, and it worked** — the order was driven through `under_review → forwarded → shipped → delivered` afterward with zero further drift on any row (previously, each new transition corrupted an earlier row's `status`). The mystery is effectively resolved by elimination: the legacy columns were the cause, even though the exact mechanism was never identified.

## Next session starts with

- **PENDING, deliberately deferred by the developer**: verify a real checkout succeeds end-to-end once deployed to Vercel. The local-only Cloudflare 403 block against Flutterwave (see Current state above) needs confirming it doesn't reproduce from production's IP range before trusting checkout is actually fixed. Nothing to do here until there's an actual deployment to test against.
- Remaining unaudited feature-specs: `18-FLASH-SALES.md`, `19-ACCOUNT-MANAGEMENT.md`, `20-ADDRESS-BOOK.md` (15, 16, and 17/Reviews are now all done — audit each before assuming unbuilt, per the established pattern).
- Two leftover test-order rows worth cleaning up whenever convenient (not urgent, no real-customer impact): the manually-promoted test order (`fcc8036c-ac25-4b35-8168-4bbc8e93d5dd`, now `delivered`) and its `order_status_history` rows, including the one still stuck showing `under_review` from the pre-migration-024 mutation bug (frozen, harmless, but inaccurate if anyone looks at it later).
- Switch back to `main` and pull, since `order-tracking`'s work is done and already merged.

### Done this session, no longer pending
Migration 024 applied and confirmed fixing the `order_status_history.status` mutation (verified across `under_review → forwarded → shipped → delivered`, no further drift). Full order lifecycle verified end-to-end through the DB, Retool, and the customer-facing tracker — including a real UI bug found and fixed (`OrderTracking.tsx` showed "Delivered" as a pulsing in-progress step instead of complete). The Retool ops app is built and working (orders queue, line items, history, status-update form). Migrations 015–017 audited via read-only schema checks — all clean, nothing partially applied.

## Open questions

- Whether `/api/reminders` and `/api/thank-you/process` (both serial loops, both `BATCH_LIMIT=50`, neither has a `maxDuration` override) should get the same Hobby-plan duration-risk fix just applied to `/api/orders/notify` — same likely root cause, not yet checked.
- Whether the "Gifvtme" vs "Givftme" naming inconsistency (open since prior sessions) has been addressed — not touched this session.
- Whether the two commits left unpushed at the end of the 2026-08-06 session (`0d0fb75`, `4aac74d`, on `main`) ever got pushed — not verified this session since all work happened on `order-tracking`, a different branch.
