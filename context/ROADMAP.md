# Gifvtme — Roadmap & Build Status

This file should be updated frequently — at minimum every time a feature moves from "not started" to "in progress" to "done." Agents should check here before assuming something is or isn't built yet.

## Build sequence (4-month plan, see `architecture/ARCHITECTURE.md`)

**Month 1 — Foundation.** Supabase schema, Sanity schema, auth, manual-entry wishlist creation.
**Month 2 — Core gifting loop.** Scraping, occasion wishlists, sharing, affiliate purchase marking.
**Month 3 — E-commerce & engagement.** Catalog browsing, cart, Flutterwave checkout, order tracking, flash sales, reminders, thank-you messages.
**Month 4 — Reviews, polish, launch.** Reviews, group gifting data model only, responsive polish, bug fixing, soft launch.

## Current status (update this section as work progresses)

### Done
- Supabase schema — migration 001 (core wishlist/occasion/reminder/purchase tables) and migration 002 (e-commerce additions: orders, order_items, order_status_history, origin fields)
- Sanity schema — supplier, occasion, collection, product (with hybrid simple/complex variant model)
- Auth — signup, login, callback, email verification page, middleware-based route protection
- Auth flow spec 01 — mobile-first onboarding, welcome, signup, login, Google OAuth, forgot password OTP, reset password, success screens, Supabase auth actions, and Next 16 `proxy.ts` route protection
- Next.js app scaffold — full route structure for dashboard, shop, account, giver-facing wishlist views
- Shared component library foundation — Button, Badge, PriceDisplay, QuantityStepper, ProductCard, ProductGrid, Navbar, Footer, MobileBottomNav, PageWrapper
- Home page — fetches occasions and featured products from Sanity, renders via ProductGrid
- Gift museum/catalog browsing — homepage flash sale/occasion/featured sections, `/occasions`, `/occasions/[slug]`, `/collections/[slug]`, `/shop`, `/search`, `/product/[slug]`, cart context, variant selector, product image gallery, related products, catalog add-to-wishlist, and newsletter capture
- Cart and Flutterwave checkout — `/cart`, `/checkout`, `/checkout/processing`, `/checkout/failed`, `/account/orders/[id]`, `/api/cart/prices`, `/api/checkout`, `/api/checkout/retry`, and `/api/flutterwave/webhook` for catalog-only orders with server-fetched Sanity price snapshots and verified webhook confirmation
- Sanity catalog schema files — supplier, occasion, collection, product, attribute option, variant attribute, product variant, including flash sale fields
- Affiliate URL transformer (Jumia, Amazon, Konga)
- Microlink scraping integration (`/api/scrape`)
- Evergreen wishlist core — dashboard auto-creation, wishlist list/detail screens, add/edit/archive/reorder external items, manual image upload wiring, title editing, and owner-checked wishlist APIs. Add-item dialog now has URL/Manual tabs wired to `/api/scrape` (Fetch → editable preview → auto fallback to manual on failure/timeout); catalog adds re-fetch title/image/price from Sanity server-side rather than trusting the client; item edits now sync evergreen `master_items` in addition to `wishlist_items`.
- Supabase schema migration 003 file — adds `sort_order` to `wishlist_items` and `master_items` (applied to the Supabase project, confirmed 2026-07-31)
- Sharing and giver flow core — receiver share settings sheet, visibility/price auto-save, invite management with Resend email fallback, `/w/[id]` shared wishlist view, giver item detail, external purchase confirmation, claimed success, intent flagging, invitee reminder opt-in, `/api/purchases`, and `/api/reminders`
- Supabase schema migration 006 file — adds intent flags, invite helper policies/functions, and shared wishlist resolver (applied to the Supabase project, confirmed 2026-07-31)
- Supabase schema migration 012 file — adds a standalone `auth.users` signup trigger backfilling `wishlist_invites.invitee_user_id` for pending email invites (applied to the Supabase project, confirmed 2026-07-31); closes the one real gap identified against `07-WISHLIST-SHARING.md` — the rest of that spec was already covered by migration 006
- Shared wishlist view (`08-SHARED-WISHLIST-VIEW.md`) gap-closing pass — the giver landing page (`/w/[id]`) was already shipped under migration 006; this pass closed the real gaps against the spec: private-vs-not-found differentiation and an archived-occasion notice (`gifvtme_migration_013_shared_wishlist_access.sql`, applied to the Supabase project, confirmed 2026-07-31), a custom not-found page, colored countdown states, server-side price stripping when `prices_visible=false`, server-side 24-hour intent-flag expiry (in addition to the existing `/api/reminders` cron cleanup), archived-catalog-item ("No longer available") handling via a Sanity batch check, the footer/CTA, per-filter empty states, the all-claimed banner, and SEO meta tags. Working shipped code that merely diverged in wording from the spec (share key as wishlist `id`, reminder-opt-in route naming/scope, list-style item grid, analytics event names) was left as-is and the spec file rewritten to document it.
- Supabase schema migration 007 file — adds `newsletter_subscribers` for catalog discount/newsletter capture (applied to the Supabase project, confirmed 2026-07-31)
- Profile management — `/account/profile` page: name/phone/avatar/default thank-you message editing, "add your name" banner, sign out, and DELETE-confirmation account deletion (blocked while an order is active). Navbar now threads `avatar_url` through for the account link.
- Supabase schema migration 008 file — adds `users.phone`, extends column grants for `avatar_url`/`phone`, and creates the public `avatars` Storage bucket with owner-folder policies (applied to the Supabase project, confirmed 2026-07-31)
- Occasion wishlist — 3-step creation (`/my-occasions/new`), pull-from-evergreen and exclusive-item flows, occasion detail page (`/my-occasions/[id]`) with edit/share/archive, manual + daily-cron auto-archive, and evergreen `master_items` reactivation after an occasion archives. Dashboard occasions section (on `/wishlists`) now splits into an active list and a collapsible "Past occasions" section, and the edit-occasion dialog notes that reminders will be rescheduled on a date change.
- Occasion reactivation prompts — Supabase schema migration 009 file adds `occasion_prompts` (applied to the Supabase project); archiving an occasion (manual or cron) best-effort creates a prompt row when it has purchased evergreen items, surfaced as a dashboard-wide `ReactivationPromptsBanner` on `/wishlists` in addition to the existing per-occasion prompt. Resolving via `/api/occasions/[id]/reactivate` marks the prompt resolved; the daily archive cron also auto-resolves prompts left unresolved for more than 30 days (items stay purchased either way).
- Supabase schema migration 010 file — fixes a `WITH ORDINALITY` + column-definition-list syntax bug in migration 005's `gifvtme_create_occasion_with_wishlist` (only surfaces at execution time, not at function creation, so it shipped silently) that made occasion creation fail with a 500 (applied to the Supabase project; occasion creation confirmed working)
- Item detail (giver view) & intent flag (`09-ITEM-DETAIL-GIVER.md`) gap-closing pass — `/w/[id]/item/[itemId]`, `/confirm/[itemId]`, `/success/[itemId]`, and the flag-intent API were already shipped under migration 006; this pass fixed real bugs and closed real gaps rather than rewriting shipped design choices. Fixed: the intent-flag RPC could never be overwritten by a second giver even after the 24h expiry window (`gifvtme_migration_014_intent_flag_fixes.sql`) — now expiry-aware, and an active flag from someone else returns a soft 200 `{ warning: 'already_flagged' }` instead of a hard error; flagging a purchased item now correctly returns 409 instead of 404; the UI now distinguishes "you flagged this" (with a working Remove link — the DELETE endpoint was previously dead code) from "someone else flagged this" (with a "Buy anyway" reveal that actually hides/shows the purchase CTAs, per spec); the intent-flag section is now hidden for the wishlist owner viewing their own item. Also fixed a functionally broken catalog purchase flow — "Add to cart" previously linked to `/checkout?item=...`, a param the checkout page never read, so the wishlist item was never actually added to cart or associated with the resulting order; it now fetches the live Sanity product (`PRODUCT_BY_ID_QUERY`), renders a variant selector when the product has variants, adds the correctly-priced line item to the cart, and threads the wishlist-item association through to `/api/checkout` via a small localStorage marker (`lib/checkout/pendingWishlistItem.ts`) so the existing server-side `wishlist_item_id` handling (already built, just never fed by the client) marks the item purchased on order completion. Item description/notes now render on the page (previously fetched but never shown). Cosmetic-only divergences (CTA copy, back-link style, origin-context badge wording, analytics event naming) were left as shipped and the spec file rewritten to document reality.
- Supabase project setup — migrations 003, 004, 005, 006, 008, 011, 012, 013, 014 and the private `wishlist-images` bucket with owner-folder storage policies confirmed applied/created by the developer (2026-07-31), resolving the multi-session open item; item-detail-giver branch was merged via PR #19 the same session
- Affiliate redirect & purchase confirmation (`10-AFFILIATE-PURCHASE-CONFIRM.md`) gap-closing pass — the confirm/success screens and `/api/purchases` were already shipped under migration 006 and already reconciled once against `09-ITEM-DETAIL-GIVER.md`; this pass audited the same code against `10`'s spec specifically. Fixed: no analytics event fired when the confirm screen loaded (added `purchase.external.confirm_screen_viewed`); `/api/purchases` collapsed "item not found" and "item already purchased" into a single 404, so a stale-page confirm surfaced a generic error instead of the friendlier, already-handled-client-side race message (split into 404 not-found/archived vs 409 already-purchased); the claimed-success screen had no item image/title and no celebration animation (added a small confirmatory item preview and a brief GSAP sparkle burst on mount). Cosmetic-only divergences (button copy, card sizing, back-link style, error-response shape) left as shipped. Flagged, not fixed: the `purchases`/`thank_you_messages` schema and `on_purchase_created` trigger have no SQL source of truth anywhere in this repo — every migration file assumes they already exist, applied to Supabase outside version control before this repo's migration numbering began.
- Reminders (`11-REMINDERS.md`) — Flow 2 (invitee opt-in) and the owner-reminder scheduling/cron scaffolding were already shipped (see migration 006/`08-SHARED-WISHLIST-VIEW.md`); this pass built the two genuine gaps the audit found: **Flow 1 (important dates)**, which was completely unbuilt — `important_dates` existed live with zero application code touching it, and `/dashboard/dates` was a static empty-state stub. Now has a full CRUD API (`/api/important-dates`, `/api/important-dates/[id]`), a real UI (`ImportantDatesClient`/`ImportantDateCard`/`ImportantDateForm`), 14-day/3-day reminder scheduling, and recurring-date advancement (Feb 29 → Feb 28 in non-leap years, with a documented drift limitation — see `lib/important-dates/server.ts`). And **real reminder delivery** — `/api/reminders` previously only queued due rows and never sent anything; it now builds and sends actual Resend emails per reminder type/source via `buildReminderEmail`/`sendReminderEmail`, with `retry_count`/`permanently_failed` handling, atomic per-row claiming (`claimed_at`) so overlapping cron runs can't double-send, a Resend idempotency key to guard retries, and a real `/api/reminders/unsubscribe` endpoint. Also closed two Flow 2 gaps: invitee reminders now reschedule when an occasion's date changes (`rescheduleInviteeRemindersForOccasion`), and the unsubscribe link actually works end-to-end. `push`-channel reminders are intentionally left queued (unprocessed) since push delivery itself is still out of scope. Rewrote `11-REMINDERS.md` to document shipped reality. `tsc`, `eslint`, and `npm test` (50/50, up from 34) all clean. Migration 015 (`gifvtme_migration_015_reminders.sql` — adds `reminders.days_before`/`retry_count`/`permanently_failed`/`sent_at`/`advance_pending`/`claimed_at` and formally creates `important_dates` with owner-only RLS) confirmed applied to the Supabase project 2026-07-31.
- Thank-you messages (`12-THANK-YOU-MESSAGES.md`) — nothing here existed before this pass — no migration, no API routes, no UI, no email builders (unlike Reminders, this wasn't a "shipped differently" reconciliation). Built: `thank_you_messages` finally gets a SQL source of truth (`gifvtme_migration_016_thank_you_messages.sql`, `CREATE TABLE IF NOT EXISTS` + defensive `ADD COLUMN IF NOT EXISTS`, matching the migration-015 pattern for `important_dates`); the genuinely-new `on_order_confirmed_thank_you` trigger creates an auto thank-you row when a wishlist-originated catalog order confirms (guarded against firing twice for the same order); `/api/thank-you/process` cron (every 5 min) sends pending `type='auto'` rows via Resend with atomic per-row claiming (`claimed_at`), a Resend idempotency key, and `retry_count`/`permanently_failed` handling identical in shape to `/api/reminders`; `/api/thank-you/[id]/personal` sends a receiver-composed message immediately and records it only on successful send (nothing retries a failed personal send — it's not queued); `/api/gifts` (renamed from the spec's `/api/dashboard/gifts` — no route in this repo is namespaced under `/api/dashboard`, and this matches the `/api/important-dates` naming convention) and the `/gifts` page (`GiftsClient`/`GiftCard`/`PersonalThankYouSheet`, same "one responsive component, no separate mobile file" rule as `ImportantDatesClient`) show all external purchases and confirmed catalog orders for the receiver's items with All/To-thank/Thanked filter tabs. **Deliberately left untouched:** the existing `on_purchase_created` trigger (external-flow auto thank-you creation) — it's already running in production and its exact definition is unverifiable from this repo with no DB access from this environment, so this pass did not risk replacing working behavior with a guessed reconstruction; only the net-new catalog-flow trigger was added. `vercel.json` was added to the repo for the first time, with cron entries for both `/api/thank-you/process` (per spec, every 5 min) and `/api/reminders` (hourly — an assumed cadence, since reminders' schedule previously lived outside this repo with no documented interval; cheap to change later). `tsc`, `eslint`, and `npm test` (59/59, up from 50) all clean. Migration 016 confirmed applied to the Supabase project 2026-08-01.
- Gift Museum & Catalog (`13-GIFT-MUSEUM-CATALOG.md`) gap-closing pass — the entire feature (occasions/collections/shop/product/search pages, Sanity schema with flash-sale fields, GROQ queries, pricing helpers, variant selector, wishlist picker) was already shipped under an earlier session (see the "Gift museum/catalog browsing" line above), so this pass audited shipped code against the spec rather than rebuilding. Fixed real gaps: `getActivePrice` (`lib/flutterwave/getActivePrice.ts`, used at checkout) and `getProductDisplayPrice` (`lib/sanity/catalog.ts`, used for display) had no guard against a Sanity data-entry error where `salePrice` is entered higher than `basePrice`/variant price — both now clamp with `Math.min`, closing a real overcharge risk (spec Edge Case #6); `/search` fired a Sanity query for any non-empty input, including 1-character queries, and passed the raw query straight into GROQ with no stripping of `"`/`*`/`\` — added `sanitizeSearchQuery`/`isSearchableQuery` (`lib/sanity/catalog.ts`) and a "Type at least 2 characters to search" state; `WishlistPickerSheet` had no "Create new occasion" link and no cap on long wishlist lists — added a link to `/my-occasions/new` and a scrollable 6-option cap; the product-detail variant selector disabled Add to cart/wishlist with no explanation when an attribute was unselected — added a "Please select a [attribute]" hint; `ProductCard`'s flash-sale badge had no countdown timer (only `ProductDetail` did) — wired `FlashSaleTimer` into the card badge; added `museum.search.product_clicked` (query, product_id, position) analytics, threaded through `ProductCard` → `ProductGrid` → `CatalogProductGrid` → `ProductExplorer`, fired only when `ProductExplorer` is given a `searchQuery` (i.e. only from `/search`, not shop/collection grids); added unit tests for `getActivePrice`/`isFlashSaleWindowActive` (`lib/flutterwave/getActivePrice.test.ts`) and `getProductDisplayPrice`/`sanitizeSearchQuery`/`isSearchableQuery` (`lib/sanity/catalog.test.ts`). Naming/architecture divergences from the spec (`museum.*` analytics event prefix vs spec's `catalog.*`, `status` enum vs `archived` boolean, submit-on-enter search form vs a live-debounced input, fragment-based GROQ queries vs the spec's inline shapes, `combinationKey` authored directly in Sanity Studio rather than computed client-side) were left as shipped — cosmetic/architectural, not functional gaps — and `13-GIFT-MUSEUM-CATALOG.md` was left as the aspirational spec text rather than rewritten, since (unlike prior gap-closing passes) the bulk of the feature already matched it. An orphaned, unmerged commit on the stale `gift-museum/catalog` branch (`a687de9`) was evaluated and deliberately not cherry-picked — it predates this branch's server-side price/availability hardening on the wishlist-add API and would have reintroduced a client-trusted-price regression. `tsc`, `eslint`, and `npm test` (82/82, up from 59) all clean.
- Search (`14-SEARCH.md`) gap-closing pass — `/search` and `PRODUCT_SEARCH_QUERY` were already shipped (mostly during the `13` pass above), so this audited shipped code against `14`'s spec rather than rebuilding. Fixed 4 real gaps: mobile had no way to search at all (`Navbar.tsx`'s search form was `hidden md:block` with no mobile equivalent) — added a mobile search icon opening a full-screen overlay (autofocus, Escape-to-close, same `/search?q=` GET form); navigating to `/search?q=` with an empty query redirected straight to `/shop`, skipping the spec's "Enter at least 2 characters" prompt entirely — removed the redirect, page now renders the prompt for both empty and 1-character queries; `museum.search.no_results` fired even when no Sanity query had actually run (query too short), conflating "searched, found nothing" with "never searched" in analytics — event now only fires when `isSearchableQuery` is true; `PRODUCT_SEARCH_QUERY` had no relevance ranking (ordered by `featured desc, _createdAt desc` only) — added GROQ `score()`/`boost()` (title weighted highest, then description, then category) with `_score` as the primary sort key, `featured`/recency as tiebreakers. Left as shipped: sanitization strips `"`, `*`, `\` rather than the spec's `"`, `*`, `~` — backslash is a more defensible character to strip than tilde, which isn't a documented GROQ special character; a spec-requested "Shop → Search results" breadcrumb was not added since no breadcrumb pattern exists anywhere else in this codebase (would be a new UI convention, not a search-specific fix). `tsc`, `eslint`, and `npm test` (82/82) all clean.
- Cart & Checkout (`15-CART-CHECKOUT.md`) gap-closing pass — cart/checkout was already shipped (see the "Cart and Flutterwave checkout" line above), so this audited shipped code against `15`'s spec rather than rebuilding. Fixed the one real gap: `POST /api/checkout` had no idempotency protection at all — a network retry or double-submit could create two orders for the same cart. Added `orders.idempotency_key` (migration 017, nullable + partial unique index) and a required `Idempotency-Key` header; the route now looks up an existing order by key before creating anything, and either re-initiates Flutterwave payment against it (via a new shared helper, `lib/checkout/reinitiatePayment.ts`, also now used by `/api/checkout/retry`) or, if already resolved, returns `payment_link: null` so the client falls through to the processing page. `CheckoutForm.tsx` derives the key from a signature of the cart contents so a resubmit of the same cart reuses it while a genuinely different cart gets a fresh one. Three other divergences from the spec text were reviewed and deliberately left as shipped rather than rewritten: the processing page polls `orders.status` directly from Supabase via RLS instead of a dedicated `/api/orders/[id]/status` (that route is documented in `API_ROUTES.md` as a possible future Retool-only endpoint, not something customer polling needs); saved addresses were never built and are now scoped to their own spec, `20-ADDRESS-BOOK.md`; and `/account/orders/[id]` serves as the confirmation page rather than a separate `/checkout/success/[orderId]` route. `15-CART-CHECKOUT.md` was rewritten to document all of this. Added test coverage that didn't exist before: `lib/checkout/validation.test.ts` (checkout Zod schema), `app/api/checkout/route.test.ts` (idempotency behavior), `app/api/flutterwave/webhook/route.test.ts` (signature verification, amount-mismatch and duplicate-webhook idempotency). `tsc`, `eslint`, and `npm test` (108/108, up from 82) all clean.
- Checkout order-creation race fixed (2026-08-06, follow-up to the pass above) — a code review of the new idempotency logic found `POST /api/checkout` still had a real concurrency bug: it inserted `orders` and `order_items` as two separate statements, so a concurrent request carrying the same `Idempotency-Key` could find the just-committed `orders` row (visible to other transactions immediately) and initiate Flutterwave payment against it before `order_items` existed — worse, if the first request's `order_items` insert then failed, its compensating delete could remove the order the second request had just started paying against. Fixed by moving both inserts into one Postgres transaction via a new RPC, `gifvtme_create_checkout_order` (migration 018, same transactional-RPC pattern as `gifvtme_create_occasion_with_wishlist`) — `POST /api/checkout` now calls this once instead of two sequential inserts, so no request can ever observe an incomplete order. Added two regression tests in `app/api/checkout/route.test.ts` asserting order_items are submitted in the same call as the order and that a losing concurrent request pays against the re-fetched, fully-committed order rather than its own failed attempt. `15-CART-CHECKOUT.md`, `API_ROUTES.md`, `DATABASE_SCHEMA.md` updated. `tsc`, `eslint`, and `npm test` (110/110, up from 108) all clean.
- Checkout idempotency-key signature widened to shipping (2026-08-06, second follow-up to the `15-CART-CHECKOUT.md` pass) — a further code review of the idempotency logic found `getCartSignature` in `CheckoutForm.tsx` only hashed cart contents and `wishlist_item_id`. Since `/api/checkout`'s replay path pays for an order using its originally-stored shipping details (it never re-reads a resubmitted body's shipping), a user who submitted checkout, didn't finish paying, went back to fix a typo'd phone number or address, and resubmitted without touching the cart would keep the same idempotency key — silently shipping the *old*, wrong shipping details. Renamed to `getCheckoutSignature` and folded in every Zod-normalized shipping field (name, email, phone, address, city, state, postal code, delivery instructions), so a shipping correction now mints a fresh key (new order) while an identical resubmission still correctly reuses one. A related suggestion to also include `preferred_payment` in the signature was evaluated and rejected — `preferred_payment` isn't stored on the order at all, `/api/checkout` always forwards the *current* request's value into Flutterwave initiation on every call (fresh or replayed), so it can never go stale; adding it to the signature would instead cause a spurious duplicate order any time someone toggled payment method between submissions. `API_ROUTES.md` and `15-CART-CHECKOUT.md` updated to document the exact signature inputs and the `preferred_payment` exclusion. `tsc`, `eslint`, and `npm test` (111/111) all clean — no new automated test added, since this repo has no component-level test harness for `CheckoutForm.tsx` yet.
- Checkout payment-initiation race fixed (2026-08-06, third follow-up to the `15-CART-CHECKOUT.md` pass) — a further code review found that, even with the order-creation race and idempotency-key fixes above, nothing stopped two concurrent calls into `reinitiateOrderPayment` (`lib/checkout/reinitiatePayment.ts`, shared by `/api/checkout`'s replay path and `/api/checkout/retry`) — a double-click on "Try again," or two idempotency-key replays racing — from each starting an independent, separately-payable Flutterwave session for the same order. The DB would still only ever mark the order `confirmed` once (the webhook's own idempotency guard), but a buyer who completed *both* payment sessions would actually be charged twice by Flutterwave. Fixed with the same `claimed_at` compare-and-set pattern `/api/reminders`/`/api/thank-you/process` already use for cron rows: `orders.payment_claimed_at` (migration 018, set at creation time so even the very first Flutterwave call is claim-protected against a concurrent replay) is atomically claimed — a conditional `UPDATE ... WHERE status IN (...) AND (payment_claimed_at IS NULL OR stale)`, checked for an affected row — before every re-initiation attempt; a losing claim returns 409 without touching Flutterwave, and the claim is released whenever a Flutterwave call fails so a genuine follow-up retry isn't blocked. Stale window is 2 minutes (vs. the cron jobs' 10), since this guards an interactive click, not a background job. Added a regression test asserting a lost claim returns 409 without calling Flutterwave; updated the existing idempotency-replay and race tests for the extra claim-step DB call. `API_ROUTES.md`, `DATABASE_SCHEMA.md`, `15-CART-CHECKOUT.md` updated. `tsc`, `eslint`, and `npm test` (112/112, up from 111) all clean.
- Vercel cron schedules fixed (2026-08-06) — `vercel.json`'s `/api/reminders` (hourly) and `/api/thank-you/process` (every 5 minutes) were failing because the project is on Vercel's **Hobby plan**, which only permits daily cron schedules. Both changed to once-daily (`0 6 * * *` and `15 6 * * *`, UTC — roughly 7am WAT). Real behavior change: automated thank-you emails now take up to ~24h to send instead of ~5 minutes, and reminder processing dropped from hourly to daily. `12-THANK-YOU-MESSAGES.md` and `API_ROUTES.md` updated to document the new cadence and the reason. Revisit if the project ever moves to a Pro plan.
- Reviews (`17-REVIEWS.md`) — built from spec, not a gap-closing pass, though the read side was partly scaffolded ahead of time: `RatingBreakdown` (`components/product/`), the `ProductReview`/`ProductReviewsSummary` types, and a `getProductReviewsSummary`/`canUserReviewProduct` pair in `lib/reviews/server.ts` already existed and were already wired into `ProductDetail`'s Reviews tab, gracefully returning empty/ineligible whenever the `reviews` table didn't exist yet. This pass added everything that scaffolding was waiting on: migration 020 (`reviews` table, `one_review_per_user_per_product` UNIQUE constraint, owner-write/public-read RLS, and — since no migration in this repo, 001/002 included, ever actually defined `update_updated_at_column()` despite the spec assuming it did — the trigger function itself, defined defensively rather than assumed); `isVerifiedPurchaser` (renamed from `canUserReviewProduct`, now exported and reused by both `/reviews/new` and `POST /api/reviews`); `POST`/`GET /api/reviews` and `PATCH`/`DELETE /api/reviews/[id]`; the `/reviews/new` submission/edit page (added to `proxy.ts`'s protected routes) with its `ReviewForm`, `StarRating`, `ReviewCard`, and `ReviewsList` components; and the GSAP bar-chart animation `RatingBreakdown` was missing (spec Edge Case #6 / Acceptance Criteria). `canLeaveReview` was redefined from "eligible" to "eligible and doesn't already have a review" — the spec's UI Requirements only mention eligibility, but its own Manual QA explicitly expects the CTA to disappear once reviewed and reappear after a delete, which eligibility alone can't satisfy. Two spec-internal contradictions were implemented literally rather than silently resolved (same call as migration 019's status-transition map): `POST /api/reviews` returns 409 on a concurrent-insert unique-violation exactly as its own Backend Logic pseudocode says, even though Edge Case #2 describes that same race resolving silently instead. A direct, ineligible visit to `/reviews/new` renders an inline "not eligible" message rather than the spec's "redirect to `/shop` with a toast" (for a missing/invalid `product_id` only) — no toast-via-redirect convention exists anywhere in this codebase (same reasoning the `14-SEARCH.md` pass used to skip a breadcrumb no other page has), so one wasn't invented for this single case. `lib/reviews/aggregates.ts` (new) holds the rating-average/breakdown math as a pure function so it's unit-testable without a database — no test in this repo mocks a `SupabaseClient`, so `isVerifiedPurchaser` itself stays untested, consistent with how its predecessor `canUserReviewProduct` was never tested either. `API_ROUTES.md`, `DATABASE_SCHEMA.md`, `FOLDER_STRUCTURE.md`, `COMPONENT_LIBRARY.md` updated. `tsc`, `eslint`, and `npm test` (139/139, up from 127) all clean. A pre-existing Turbopack production-build error ("Invalid segment configuration export detected") was confirmed present on this branch before this session's changes (isolated via `git stash -u`) — unrelated to Reviews, not fixed here, flagged for a separate session.
- Order Tracking & Fulfillment (`16-ORDER-TRACKING.md`) — built from spec, not a gap-closing pass (only `/account/orders/[id]` existed before, as the post-checkout confirmation screen — see "Cart and Flutterwave checkout" above). New: migration 019 formally owns `order_status_history` (`CREATE TABLE IF NOT EXISTS` + defensive `ADD COLUMN IF NOT EXISTS`, same pattern as migrations 015/016), adds tracking columns to `orders` (`tracking_number`, `tracking_url`, `carrier_name`, `estimated_delivery`, `status_change_notes`), the `on_order_status_changed` and `validate_order_status_transition` triggers (the backward/skip-step guard, implemented verbatim from the spec's own transition map), owner-scoped RLS on `order_status_history` (no customer read path existed before this feature), and an `ON DELETE SET NULL` fix on `orders.wishlist_item_id`'s FK (spec Edge Case #6). `lib/orders/` (types, `isValidOrderStatusTransition` — a JS mirror of the DB trigger with unit tests, `buildOrderStatusEmail`, `getOrdersForUser`/`getOrderDetail`), `GET /api/orders`, `GET /api/orders/[id]`, and the `POST /api/orders/notify` cron (same atomic-claim/retry/`permanently_failed` pattern as `/api/reminders` and `/api/thank-you/process` — not in the spec's literal `Database Changes` SQL, but required by its own Edge Case #4 and by this repo's established concurrency-safety convention for every cron). UI: `/account/orders` (Active/Completed/Cancelled tabs, `OrderList`/`OrderCard`/`OrderStatusBadge`), and `/account/orders/[id]` rebuilt around a 4-step `OrderTracking` progress bar (GSAP pulse on the current step, red banner in place of the tracker for `cancelled`/`refunded`), tracking section, and an expandable status-history timeline — this **replaces** the old one-shot `OrderConfirmationScreen` (deleted; it had no remaining callers) since the new page now covers that same "just paid" moment as its `confirmed` state. Two spec-text gaps closed rather than followed literally: the cron runs daily (`vercel.json`, `30 6 * * *`) instead of every 5 minutes, same Vercel Hobby-plan constraint as reminders/thank-you above; and `delivered`'s email CTA links to the order instead of a review flow. One spec-internal inconsistency was originally left as the spec's own literal SQL rather than "fixed" by guessing: Functional Requirement #3's prose says cancellation is allowed "from any non-terminal status," but the transition map given in Backend Logic only allowed `cancelled` from `confirmed`/`under_review`/`forwarded`/`shipped` (not from `pending_payment`/`payment_failed`) — implemented the map verbatim, pending an explicit business decision (see the follow-up entry below, resolved 2026-08-12). Also left as spec text rather than built: `order.status_changed`/`order.customer_notified` analytics (no instrumentable app code path — Retool writes `orders.status` directly via service role, and there's no established server-side analytics sink beyond the client-facing `trackEvent()`/`/api/analytics` pair); `orders.list.viewed`, `order.detail.viewed`, and `order.tracking_link_clicked` are wired. Added `lib/orders/statusTransitions.test.ts` and `lib/orders/buildOrderStatusEmail.test.ts`. `tsc`, `eslint`, and `npm test` (127/127, up from 112) all clean.
- Cancel-from-pending-payment transition gap resolved (2026-08-12, follow-up to the `16-ORDER-TRACKING.md` pass) — a pasted review finding flagged the FR3-vs-transition-map contradiction above as unresolved and asked for an explicit business decision before touching the code, rather than guessing. Asked; the developer confirmed the broader FR3 rule: cancellation should be allowed from `pending_payment`/`payment_failed`, not just from `confirmed` onward, so an order that never completed payment can be explicitly closed out instead of sitting unpaid indefinitely. `gifvtme_migration_021_cancel_from_pending.sql` redefines `validate_order_status_transition()` (via `CREATE OR REPLACE FUNCTION`, safe to run whether or not migration 019 itself has been confirmed applied — still unconfirmed as of this entry) to add `pending_payment`/`payment_failed` → `cancelled`; `lib/orders/statusTransitions.ts`'s `VALID_TRANSITIONS` and `16-ORDER-TRACKING.md`'s own Backend Logic map updated to match, plus a new regression test. `tsc`, `eslint`, and `npm test` (140/140, up from 139) all clean.
- `order_status_history` schema mismatch found and fixed (2026-08-12, second follow-up to the `16-ORDER-TRACKING.md` pass) — a pasted review finding asking to confirm migration 019's live-application status turned out to have read access to the actual Supabase project (the service-role key already present in `.env.local`), so this was checked directly via read-only REST calls instead of staying an open question. Result: migration 019 **is** applied, but broken. `order_status_history` pre-existed with `old_status`/`new_status`/`changed_by`/`created_at` — not the `status`/`changed_at` migration 019 assumed. Those two columns were declared only inside migration 019's `CREATE TABLE IF NOT EXISTS order_status_history (...)` block, which silently no-opped against the already-existing table, so they were never added — unlike `customer_notified`/`notes`/`retry_count`/`permanently_failed`/`claimed_at`, each its own explicit `ADD COLUMN IF NOT EXISTS` and confirmed present live. Consequence: `handle_order_status_changed()` inserts into `(order_id, status, changed_at, notes)`, so **every future order status change will fail** with a real Postgres error and roll back, until fixed — very likely never triggered for real yet since Retool isn't connected to this project (`ROADMAP.md` "Not started"). `gifvtme_migration_022_order_status_history_columns.sql` adds the two missing columns (nullable, best-effort backfilled from `new_status`/`created_at`) — **written but not applied**, since this environment only has read access via the REST API, not a DDL/write path. `memory.md` updated with the verified facts in place of the earlier "not confirmed" placeholder. `DATABASE_SCHEMA.md` updated to document the table's actual live shape. Must be applied before any Retool status-change testing.
- Migration 022 applied; checkout found broken the same way, live-debugged (2026-08-12) — the developer confirmed migration 022 applied cleanly (re-verified via the same read-only REST checks: `order_status_history.status`/`changed_at` now present). Setting up Retool (`16-ORDER-TRACKING.md`'s ops interface) surfaced a real checkout bug next: `POST /api/checkout` was returning 502 with `PGRST204: Could not find the 'payment_claimed_at' column`. Same root cause as the `order_status_history` finding above but on `orders` — migration 018 (`gifvtme_migration_018_checkout_atomic_order.sql`) had only been *partially* applied: `gifvtme_create_checkout_order()` and its `GRANT` existed and worked (confirmed via a safe read-only RPC probe that hit the function's own internal `auth.uid()` guard without inserting anything), but the `ALTER TABLE orders ADD COLUMN IF NOT EXISTS payment_claimed_at` line above it had not run — likely a partial copy-paste into the Supabase SQL editor at some point before this repo's migration history was fully tracked. Unlike migration 019/022, no new migration file was needed: 018 is already fully idempotent (`ADD COLUMN IF NOT EXISTS` / `CREATE OR REPLACE FUNCTION` / `GRANT`), so the fix was just re-running the existing file in full — developer confirmed this resolved the `PGRST204` error. That surfaced a second, unrelated gap while debugging: both `POST /api/checkout` and `lib/checkout/reinitiatePayment.ts` swallowed the real reason a Flutterwave initiation failed, logging nothing (or a generic fallback string) instead of `payment.error`. Fixed by adding `console.error` calls at both call sites, and by rewriting `initiateFlutterwavePayment` (`lib/flutterwave/index.ts`) to read the raw response body via `response.text()` and surface `` `Flutterwave HTTP ${status}: ${rawBody}` `` whenever Flutterwave's JSON has no `message` field (previously collapsed to a useless generic string). That fix immediately paid off: the real error turned out to be a Cloudflare "Attention Required" 403 challenge page in front of Flutterwave's API — a network/IP-level block against the developer's local machine, not an application bug. A bare curl to the same endpoint from this session's own sandbox got a clean `401` from Flutterwave's actual API with no Cloudflare challenge, ruling out a blanket policy against non-browser clients. Left unresolved, deliberately: this only reproduces from the developer's local dev network, and deploying to Vercel puts requests through a completely different IP range, so verification is deferred to post-deploy rather than debugged further locally.
- `order_status_history` still broken after migration 022 — `new_status` NOT NULL gap found and fixed (2026-08-13) — manually moving a test order to `confirmed` (to unblock Retool testing without waiting on the Flutterwave/deployment item above) hit a fresh error: `null value in column "new_status" ... violates not-null constraint`. Migration 022 fixed `status`/`changed_at` but missed that the pre-existing `new_status` column is `NOT NULL` with no default — the trigger never supplied it. Confirmed this time via PostgREST's own OpenAPI schema (`GET /rest/v1/`, parsed for the full `order_status_history` definition) rather than the column-by-column probing used for the two earlier findings — a strictly better technique now established for verifying live schema state. Full live column set is now fully known: `id`, `order_id`, `changed_by`, `old_status`, `new_status`, `note` (singular, distinct from `notes`), `created_at`, `customer_notified`, `notes`, `retry_count`, `permanently_failed`, `claimed_at`, `status`, `changed_at`. `gifvtme_migration_023_order_status_history_new_status.sql` (`CREATE OR REPLACE FUNCTION handle_order_status_changed()`) now also inserts `old_status`/`new_status` from `OLD.status`/`NEW.status` — `old_status` isn't `NOT NULL` so wasn't strictly required, but populated anyway since the value is free at insert time and leaving it permanently null forever wasn't worth the alternative. `DATABASE_SCHEMA.md` updated with the complete confirmed column list. **Developer confirmed applied and working 2026-08-13**, re-verified via read-only REST checks: one order now sits in `confirmed`, and `order_status_history` has a real row (`old_status: pending_payment`, `new_status: confirmed`, both timestamps populated) — the trigger fired correctly end to end. This is the third order_status_history migration in two days (019 → 022 → 023); still worth staying alert for a fourth gap the first time a *different* transition (e.g. `shipped`, `cancelled`) actually fires, rather than assuming `confirmed` working proves every path is clean.
- **`order_status_history.status` observed mutating on already-written rows — cause unknown, defensive fix applied (2026-08-13).** After a second real transition (`confirmed` → `under_review`) via the Retool app, the *first* history row's `status` column had silently changed from `confirmed` (correct at the time, confirmed by an immediate read right after it was written) to `under_review` (the order's new, unrelated current status) — while that same row's `old_status`/`new_status` stayed correct (`pending_payment`/`confirmed`). Exhaustively ruled out as the cause, each confirmed live rather than assumed: the Retool app's own queries (all four inspected directly — only one write query exists, and it only touches `orders`); a rogue DB trigger (`information_schema.triggers` on both `orders` and `order_status_history` shows exactly the four expected triggers, none matching); Postgres RULES (`pg_rules` — zero); a generated column (`information_schema.columns` — `is_generated: NEVER`); `pg_cron` (extension not installed); and the live trigger function body itself, fetched via `pg_get_functiondef()`, which matches migration 023 verbatim. The developer confirmed no manual SQL was run against this table either. No cause found. **Defensive fix, not a root-cause fix:** every app-code read of `order_status_history.status` now selects `status:new_status` instead (PostgREST column aliasing — the JSON key stays `status` so nothing downstream changes shape), since `new_status` has been correct in every single observation so far and `status` has not. Changed: `lib/orders/server.ts`'s `ORDER_DETAIL_SELECT` (feeds the customer-facing `/account/orders/[id]` history timeline) and `app/api/orders/notify/route.ts`'s `PENDING_HISTORY_SELECT` **and** its `.in("status", ...)` filter → `.in("new_status", ...)` (the filter was querying the live, unreliable column directly regardless of select aliasing — a separate mechanism that needed its own fix). Real risk this closes: the notify cron could have silently skipped a customer-facing status email (if a row's `status` drifted to an internal-only value like `under_review` before the cron ran) or sent the wrong template. Not yet checked: whether `notes`/`changed_at`/`customer_notified`/`retry_count`/`permanently_failed`/`claimed_at` — all migration-022-added plain columns in the same category as the affected `status` — are subject to the same unexplained mutation. Revisit if this happens again with a different column, since that would rule out something specific to `status` and point at something structural.
- **Legacy `old_status`/`new_status`/`changed_by`/`note` columns dropped rather than root-caused (2026-08-13)** — developer call: carrying two overlapping status models (the app's own `status`/`changed_at`/`notes` alongside the pre-existing `old_status`/`new_status`/`changed_by`/`note`) wasn't worth it, especially once it was the proximate cause of migration 023's workaround and a plausible (never confirmed) contributor to the `status`-mutation mystery above. `gifvtme_migration_024_drop_legacy_status_columns.sql` drops all four and restores `handle_order_status_changed()` to the literal single-column trigger `16-ORDER-TRACKING.md` always specified — no more populating `old_status`/`new_status` just to satisfy a `NOT NULL` constraint that no longer exists. The defensive `status:new_status` aliasing added minutes earlier in `lib/orders/server.ts` and `app/api/orders/notify/route.ts` (both the select and the `.in()` filter) was reverted back to plain `status` in the same change, since `new_status` won't exist once this migration runs. **Written, not yet applied** — once it is, worth triggering one more status change to confirm `status` behaves normally again; if it still mutates, that rules out the legacy columns as the cause and the mystery stays open. `DATABASE_SCHEMA.md` rewritten to describe the clean final shape, with the 019→022→023→024 history condensed into one paragraph rather than three.
- **Migration 024 confirmed fixing the mutation, and the full order lifecycle verified end-to-end (2026-08-17).** Developer applied 024, then drove the same test order through `under_review → forwarded → shipped` in Retool. Each new history row (`forwarded`, `shipped`) stayed correct through every subsequent transition — no further drift, unlike the pre-024 behavior where an earlier row's `status` changed each time a new one was written. Only the original pre-024 row is still wrong (frozen at `under_review` instead of `confirmed`) since nothing retroactively repairs old data — acceptable, it's test data. Also verified the customer-facing side, not just the database: `orders.tracking_number`/`carrier_name`/`tracking_url`/`estimated_delivery` all persisted correctly from Retool's update form, and `/account/orders/[id]` correctly rendered the 4-step tracker with `Shipped` as the current step. This is the first time this session Order Tracking was confirmed working from the DB trigger through Retool through the actual customer-facing page, not just via isolated schema checks.
- **`OrderTracking.tsx` "delivered" step rendered wrong — found via that same end-to-end test, fixed (2026-08-17).** Once the test order reached `delivered`, the 4-step tracker showed the first three steps checked but the final "Delivered" step as a pulsing, unchecked "4" — because the component's `isCurrent`/`isComplete` logic always treated the step matching the order's current status as "current" (pulsing outline, bare number), even when that status is the *last* step with nothing further to progress toward. Fixed: when the current step is the final one (`delivered`), it now renders as complete (checkmark, filled, no GSAP pulse) instead of perpetually "in progress." `tsc` and `npm test` (140/140) clean.
- Flash Sales (`18-FLASH-SALES.md`) gap-closing pass (2026-08-19) — most of this feature was already shipped during the Gift Museum/Catalog (`13`) pass (Sanity flash-sale fields, `getActivePrice`/`isFlashSaleWindowActive`, `FLASH_SALE_PRODUCTS_QUERY`, checkout's server-authoritative pricing, `FlashSaleTimer`/`FlashSaleBanner` skeletons wired into `ProductCard`/`ProductDetail`/home); `ROADMAP.md` had only flagged "Dedicated flash sale page UI" as not started, which undersold the real gap list. This pass closed the actual gaps. **Architecture decision, confirmed with the developer rather than guessed:** the spec's 5-minute checkout grace period ("honor the sale price if an order was created before saleEndTime") assumes an order can pre-date the price check; in this codebase order creation and price computation happen in the same `POST /api/checkout` call, so that condition can never hold. Redefined as a pure time-based window instead — `getActivePrice`/`isWithinGracePeriod` (`lib/flutterwave/getActivePrice.ts`) now honors `salePrice` for any checkout priced within 5 minutes after `saleEndTime`, full stop, no order-age check (a client-supplied timestamp was considered and rejected as spoofable). Built: Sanity Studio `salePrice < basePrice` validation (previously only enforced downstream in code, not authored in the schema); the grace period itself, with new unit tests; `FlashSaleTimer` threshold-based formatting (`H:MM:SS` ≥1hr, amber `MM:SS` <1hr, red `MM:SS` + GSAP pulse ≤60s — the pulse animation's exact shape was left as shipped, cosmetic-only divergence from spec pseudocode) and a `disableUrgencyColor` prop for callers on a brand-red background, where the new red/amber text would otherwise be invisible; `FlashSaleNavbarStrip` (new), fed by one more lightweight Sanity fetch added to `PageWrapper.tsx` (the shared server component every public page already routes through) and threaded down through `PublicPageShell` → `Navbar`, same pattern as `cartCount`/`userName`; the dedicated `/flash-sale` page (`app/flash-sale/page.tsx`, `revalidate = 30`) reusing `ProductExplorer` (same grid/filter/sort/load-more component `/shop` uses) against a new `/api/flash-sale/products` load-more endpoint and a new `FLASH_SALE_PRODUCTS_COUNT_QUERY` — this closes a dead `/flash-sale` link that `FlashSaleBanner`/`/shop` already pointed at; `PriceDisplay` gained an `isOnFlashSale` prop (brand-red price) and `ProductCard`'s sale badge moved from top-left to bottom-left per spec (z-indexed above the card's hover-reveal overlay); and `POST /api/checkout` now detects when its server-computed price differs from the client's submitted `display_price` (flash sale ended mid-checkout), returning `price_changed`/`price_changes` — the order is still created at the correct price either way, but `CheckoutForm.tsx` now shows a new `PriceChangeDialog` confirming the new price before redirecting to the Flutterwave payment link, instead of silently redirecting. Left as shipped rather than rebuilt: `museum.*` analytics event naming (established convention, not a gap) and the mobile flash sale strip sharing the same below-header placement as desktop rather than a separate above-bottom-nav position (`MobileBottomNav` is a separate fixed component; not worth duplicating layout logic for this pass). `18-FLASH-SALES.md` rewritten to document shipped reality. `tsc`, `eslint`, and `npm test` (146/146, up from 140) all clean.

### In progress / partially done
- Context file system (this folder) — being built out

### Not started
- Retool setup against production Supabase
- Sanity Studio deployment
- Push notification delivery for reminders (`reminders.channel` supports `push` and the cron would count due rows, but no scheduling function creates them — `REMINDER_CHANNELS` in `lib/reminders/constants.ts` is `email`-only until push infrastructure, e.g. web push subscriptions or FCM/APNs, actually exists; scheduling a channel with no delivery path would just accumulate undeliverable rows forever)

## Explicitly deferred to v2 — do not build without an explicit decision

Group gifting payment flows, custom admin dashboard (Retool is the v1 interface), in-app wallet, native mobile apps, multi-currency support, automated supplier API integration, social feed.

## How to use this file

Before starting work on a feature, check whether it's listed as done, in progress, or not started here. If you complete something, move it to the correct section in the same edit. If you discover something listed as "done" is actually broken or incomplete, correct this file rather than leaving it inaccurate for the next session.
